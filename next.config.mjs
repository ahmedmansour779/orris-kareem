/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['drive.google.com', 'orris-next.vercel.app', 'live.staticflickr.com', 'flic.kr', 'cms.orrisarena.com'],
    },
};

export default nextConfig;